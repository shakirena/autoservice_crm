function LoginPage() {
  return (
    <main data-testid="login-page">
      <h1 data-testid="login-title">AutoService CRM</h1>
      <p data-testid="login-subtitle">Sign in to continue</p>
      {/* Login form implemented in feature-auth */}
    </main>
  )
}

export default LoginPage
